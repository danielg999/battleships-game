namespace Battleships
{
    /// <summary>
    /// Direction to looking for a points to set ship on board.
    /// </summary>
    public enum Direction
    {
        Up,
        Down,
        Left,
        Right
    }
}